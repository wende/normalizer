{
  "name": "normalizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Normalizer — a normal/texture-map generator with a dependency-free JS core shared by the browser app and a Node CLI.",
  "license": "GPL-3.0",
  "scripts": {
    "build": "vite build",
    "dev": "vite --port 8765 --strictPort",
    "test": "node tests/specular.test.js && node tests/parallax.test.js && node tests/occlusion.test.js && node tests/flat-plane-offset.test.js",
    "test:e2e": "playwright test --config web/playwright.config.js"
  },
  "dependencies": {
    "pngjs": "^7.0.0",
    "preact": "^10.22.0",
    "vite": "^5.4.0"
  },
  "devDependencies": {
    "@locator/babel-jsx": "^0.5.1",
    "@playwright/test": "^1.61.1",
    "@preact/preset-vite": "^2.10.5"
  }
}
